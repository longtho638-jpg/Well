# 🚀 WELLNEXUS PRODUCTION HANDOVER

**Ngày bàn giao:** 13/01/2026
**Phiên bản:** 1.0.0-production
**Trạng thái:** Sẵn sàng Go-Live (10x Deep Cleaned)

---

## 1. TỔNG QUAN HỆ THỐNG
Dự án đã được thanh lọc toàn bộ dữ liệu giả (Mock Data) và chuyển sang sử dụng API thật 100%.
- **Frontend:** React + Vite + Tailwind (tối ưu hóa bundle).
- **Backend Services:**
    - **Auth:** Supabase Auth (Đã khóa chế độ Demo Login).
    - **Database:** Supabase PostgreSQL (Thay thế hoàn toàn Mock Data).
    - **Wallet/Money:** Logic xử lý ví đã được chuyển từ Firebase sang Supabase để đồng nhất.
- **Security:**
    - Đã vá lỗ hổng đăng nhập Demo (`useAuth.ts`).
    - Đã xóa mật khẩu mặc định (`NetworkTree.tsx`).
    - Đã chặn XSS trong Dashboard (`wow-dashboard.html`).

---

## 2. HƯỚNG DẪN VẬN HÀNH (ADMIN)

### A. Quản trị Người dùng & Cây hệ thống
- Truy cập Route: `/admin/network` (cần đăng nhập quyền Admin).
- **Thêm thành viên mới:** Bắt buộc nhập mật khẩu thủ công (không còn mặc định `Password123!`).

### B. Quản lý Ví & Hoa hồng
- Dữ liệu ví (`shop_balance`) được lấy trực tiếp từ bảng `users` trong Supabase.
- Lệnh rút tiền (`requestPayout`) sẽ tạo một bản ghi `pending` trong bảng `transactions`.

---

## 3. HƯỚNG DẪN DEPLOY

### A. Chuẩn bị Biến môi trường (.env)
Trên server (Vercel/Netlify), hãy thiết lập các biến sau:

```env
VITE_SUPABASE_URL=https://<project-id>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>
# Các biến Firebase (nếu còn dùng cho tính năng phụ, nếu không có thể bỏ)
VITE_FIREBASE_API_KEY=...
```

### B. Lệnh Deploy
**Vercel:**
```bash
vercel --prod
```

**Firebase Hosting:**
```bash
npm run build
firebase deploy --only hosting
```

---

## 4. CÁC ĐIỂM CẦN LƯU Ý (POST-LAUNCH)
1.  **Supabase Realtime:** Hiện tại `walletService` đang lắng nghe thay đổi ví qua Realtime. Hãy đảm bảo bật "Replication" cho bảng `users` trong Supabase Dashboard.
2.  **Referral Logic:** Logic F1/F2 đang chạy tốt. Nếu muốn mở rộng F3-F7, cần tối ưu lại câu truy vấn SQL (dùng Recursive CTE).

---
*Generated by Claudekit Agent - 10x Deep Audit*
